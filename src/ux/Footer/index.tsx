"use client";
import "@/styles/ux/footer.scss";
import React, { memo } from "react";

const Footer = memo(() => {
  return <footer>links und so</footer>;
});

Footer.displayName = "Footer";

export default Footer;
